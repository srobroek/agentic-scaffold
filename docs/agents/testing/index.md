# Testing

<!-- BEGIN GENERATED: testing -->
No test suite exists yet. `just check` is the acceptance test: it renders every
profile into a temporary directory and runs that project's own build.
<!-- END GENERATED: testing -->

## What counts as tested

A layer is tested when a profile using it renders and the rendered project's own
build passes. Rendering alone does not count.

