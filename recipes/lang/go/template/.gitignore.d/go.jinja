# go
{% if go_vendor -%}
# vendor/ is committed here, so nothing conditional is ignored.
{%- else -%}
# The upstream gh:Go template covers the binaries and test output. Vendor mode is the choice it
# cannot express.
vendor/
{%- endif %}
