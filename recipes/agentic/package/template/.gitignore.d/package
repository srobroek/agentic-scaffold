# OMP and Claude plugin install state, written when this repository is added as a local
# marketplace source during testing. The installs themselves live under ~/.omp/plugins/;
# these are what a run leaves beside the source it read.
installed_plugins.json
omp-plugins.lock.json

# A plugin shipping an extension module declares real dependencies in its package.json.
# The module sources are committed; the install tree is not.
node_modules/
