{#-
  .release-please-manifest.json -- release-please's record of the last released
  version per path. It bumps these; the initial values match the 0.1.0 in each
  apm.yml.
-#}
{
  ".": "0.1.0",
  "packages/{{ resolved_package }}": "0.1.0"
}
