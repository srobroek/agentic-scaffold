{
  "name": "project-scaffold-update",
  "version": "0.1.0",
  "description": "Change a repository that already exists: adopt a layer it does not have, re-render a layer whose template moved, or retrofit onto a repository that predates this tool. copier update cannot run here, so the method is re-render plus git diff.",
  "author": {
    "name": "Sjors Robroek"
  },
  "license": "Apache-2.0",
  "skills": "./.apm/skills"
}
