# CDK and projen artefacts.

# The synthesised CloudFormation output. Regenerated by `cdk synth`, and a committed
# copy goes stale silently against the constructs that produce it.
cdk.out/
*.d.ts.map

# CDK context, which caches AMI ids and availability zones per account. Committing it
# pins a lookup to whichever account first ran the synth.
cdk.context.json

# projen writes .projen/ from .projenrc.ts. It IS committed, deliberately, so a fresh
# clone can run tasks before the first synth; only the dependency tree is ignored.
