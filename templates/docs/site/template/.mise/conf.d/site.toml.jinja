# .mise/conf.d/site.toml -- mise reads every file in this directory, so each layer pins
# its own tools without a merge step.
#
# Astro 7 needs node 22.12 or later.
[tools]
node = "{{ node_version }}"
