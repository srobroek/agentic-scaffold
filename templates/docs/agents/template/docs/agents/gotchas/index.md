# Gotchas

Failures found by running a tool, where reading its documentation would not have
predicted the failure. Record the cause, not the symptom alone.

Hand-written, with no generated block. One leaf per language plus `ci.md` and
`tooling.md`.
