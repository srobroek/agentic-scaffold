---
name: {{ resolved_package }}
description: {{ resolved_description }}
---

# {{ resolved_package }}

Replace this body with the skill's instructions. The frontmatter `description` is
what an agent matches against to decide whether to load the skill, so it states
what the skill does and when to use it, not what it is.

## Structure

Long skills split their detail into `references/*.md` beside this file and link to
them, so the always-loaded body stays short. This starter ships none; add them as
the skill grows.
