# Agent index artefacts: regenerable, large, and each one feeds the next. An
# unignored artefact gets packed into the following artefact, which measured 38
# percent of one repository's whole pack.
repomix-full.xml

# `just pack-code` writes this one. An unignored artefact gets packed into the next
# artefact, which measured 38 percent of one repository's whole pack.
repomix-code.xml
graphify-out/
.serena/

# repomix's default output names, used when --output is omitted. Two of these,
# 40MB together, were committed by accident during a tuning sweep.
/repomix-output.xml
/repomix-output.md
/repomix-output.txt
/repomix.xml
